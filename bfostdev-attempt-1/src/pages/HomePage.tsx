import React from 'react';

const HomePage = () => {
    return (
        <React.Fragment>
            <h2>This is the HomePage</h2>
        </React.Fragment>
    );
};

export default HomePage;